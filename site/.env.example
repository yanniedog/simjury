# Copy values from AustralianRates `.env` (same Cloudflare account).
# Used by: npm run site:deploy (wrangler reads these from the environment).
#
# CLOUDFLARE_API_TOKEN=   # Workers Scripts Edit (+ custom domains). DNS Edit not required —
#                         # Workers custom domains create DNS records automatically.
# CLOUDFLARE_ACCOUNT_ID=f3250f7113cfd8c7f747a09f942ca6d0
