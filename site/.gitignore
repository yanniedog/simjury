node_modules/
.wrangler/
.dev.vars
.env
dist/
# Synced from pilot/src/main/resources/cases (single source of truth) — see scripts/sync-cases.mjs
public/cases/
# Built from site/app (Vite) by `build:app` — see site/package.json
public/today/
# Build-generated whitelist imported by the Worker.
src/narration-manifest.generated.js
