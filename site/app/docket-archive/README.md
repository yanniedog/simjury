# Archived Daily Docket cases

Cases moved out of the live `docket/` queue during the featured-docket curation.
They remain in the repository for reference and possible future re-feature; they
are **not** bundled by `import.meta.glob('/docket/*.json')` and are not validated
by `npm run validate:cases`.
