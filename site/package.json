{
  "name": "simjury-web",
  "private": true,
  "type": "module",
  "description": "simjury.com landing + install site (Cloudflare Workers static assets)",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "sync": "node scripts/sync-cases.mjs",
    "install:app": "npm --prefix app ci",
    "build:app": "npm --prefix app run build",
    "prebuild:assets": "node scripts/sync-cases.mjs",
    "build:assets": "npm run build:app",
    "predev": "npm run install:app && npm run build:assets",
    "dev": "wrangler dev",
    "predeploy": "npm run install:app && npm run build:assets",
    "deploy": "wrangler deploy",
    "precheck": "npm run install:app && npm run build:assets",
    "check": "npm run guard:cloudflare && wrangler deploy --dry-run",
    "guard:cloudflare": "node scripts/assert-cloudflare-static-only.mjs"
  },
  "devDependencies": {
    "wrangler": "4.113.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
